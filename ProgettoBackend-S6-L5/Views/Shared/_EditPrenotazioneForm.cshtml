
@model EditPrenotazioneViewModel

<form id="editPrenotazioneForm">
    <input type="hidden" asp-for="PrenotazioneId" />

    <div class="mb-3">
        <label for="dataInizio" class="form-label">Data Inizio</label>
        <input id="dataInizio" type="date" class="form-control" asp-for="DataInizio" value="@Model.DataInizio.ToString("yyyy-MM-dd")" />
    </div>
    <div class="mb-3">
        <label for="dataFine" class="form-label">Data Fine</label>
        <input id="dataFine" type="date" class="form-control" asp-for="DataFine" value="@Model.DataFine.ToString("yyyy-MM-dd")" />
    </div>
    <div class="mb-3">
    <label for="stato" class="form-label">Stato</label>
    <select id="stato" class="form-control" asp-for="Stato">
        <option value="In Attesa">In Attesa</option>
        <option value="Confermata">Confermata</option>
        <option value="Annullata">Annullata</option>
    </select>
</div>
    <div class="mb-3">
        <label for="clienteId" class="form-label">Cliente</label>
        <select id="clienteId" class="form-control" asp-for="ClienteId">
            <option value="">Seleziona un cliente</option>
            @foreach (var cliente in (List<ProgettoBackend_S6_L5.Models.Cliente>)ViewBag.Clienti)
            {
                <option value="@cliente.ClienteId">@cliente.Nome @cliente.Cognome - @cliente.Email</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label for="cameraId" class="form-label">Camera</label>
        <select id="cameraId" class="form-control" asp-for="CameraId">
            @foreach (var camera in (List<ProgettoBackend_S6_L5.Models.Camera>)ViewBag.Camere)
            {
                <option value="@camera.CameraId">@camera.Numero - @camera.Tipo</option>
            }
        </select>
    </div>
    <button class="btn btn-success" type="button" onclick="sendEditPrenotazioneForm()">Save</button>
</form>
